历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的书籍名称（item_names）。
1. 如果**当前用户问题**中明确提到了书籍名称，请提取出来。可能有一个或多个，但不能重复。
2. 如果**当前用户问题**使用了明确的指代词（如「这个」「它」「这本书」「刚才那本」），
   可以结合历史会话做指代消解，确定书名。
3. 如果**当前用户问题**里既没有书名、也没有上述指代词，item_names 必须返回空列表 []。
   特别注意：**绝不能因为历史会话里出现过书名，就把它当作本轮问题的书名。**
   例如历史里聊过《斗破苍穹》，本轮用户只说「你好」「谢谢」「在吗」这类寒暄，
   或问的是与书籍无关的问题，item_names 都必须是 []。
   历史只用于消解代词，不能用于补充本轮问题里根本没有的书名。
4. 请重新改写用户的问题（rewritten_query），使其成为独立完整的问题。
   若本轮没有书名，就按原问题改写成通顺的独立问句，**不要硬塞书名**。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["书籍A", "书籍B"],
    "rewritten_query": "关于书籍A和书籍B，...",
    "is_chitchat": false
}}

其中 is_chitchat 表示本轮问题是否属于「与书籍无关的寒暄/闲聊」：
若用户只是打招呼、道谢、寒暄，或聊与查书/读书/听书完全无关的话题，is_chitchat 为 true；
任何与书籍、作者、推荐、听书、读书内容相关的问题（哪怕没有具体书名），is_chitchat 都为 false。